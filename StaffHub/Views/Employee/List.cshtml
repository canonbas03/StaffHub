@model IEnumerable<StaffHub.Models.Employee>

    <h1>List of all:</h1>
    <a asp-action="Add">Add new</a>
    @foreach(var emp in Model )
{
    <p>@emp.FirstName @emp.LastName @emp.Salary @emp.Role.Name @emp.Role.Department.Name</p>
}